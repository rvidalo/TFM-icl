import { FiltroNegociosPipe } from './filtro-negocios.pipe';

describe('FiltroNegociosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroNegociosPipe();
    expect(pipe).toBeTruthy();
  });
});
